.env
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.ruff_cache/
.mypy_cache/
dist/
build/
*.egg-info/
.venv/
venv/
.DS_Store
*.log
data/
data_cache/
.claude/settings.local.json

# R13 research workspace — local DuckDB working files
research/duckdb/

# R8 trained model artifacts — produced on prod by
# scripts/train_strategy_classifier.py, persisted in the
# ./models docker-compose volume. Pickles can be 1-10 MB binary;
# train each environment from its own labels.
models/

# PLAN-UIA-001 — JSX precompile output (scripts/build_dashboard.mjs)
node_modules/
static/dashboard/dist/
# Playwright outputs
test-results/
playwright-report/
docker-compose.override.yml
